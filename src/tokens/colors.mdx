import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { colors } from './colors';

<Meta title="Colors" />

<ColorPalette>
  {Object.entries(colors).map(([name, value]) => (
    <ColorItem key={name} name={name} colors={value} />
  ))}
</ColorPalette>
